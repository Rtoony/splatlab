[Service]
# Parent cgroup enforces one combined budget with the main SplatLab service.
Slice=splatlab.slice
# The user manager lacks cpuset delegation, so enforce the same CPU set here.
CPUAffinity=8-15
